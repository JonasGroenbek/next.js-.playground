const Text = (props) => {
    return <p style={{

    }}>{props.children}</p>
}

const Small = (props) => {
    return <small style={{

    }}>{props.children}</small>
}

const H1 = (props) => {
    return <h1 style={{
        
    }}>{props.children}</h1>
}

const H2 = (props) => {
    return <h2 style={{

    }}>{props.children}</h2>
}

const H3 = (props) => {
    return <h3 style={{

    }}>{props.children}</h3>
}

const Button = (props) => {
    return <button style={{

    }}>{props.children}</button>
}

export {Text, Small, H1, H2, H3, Button}